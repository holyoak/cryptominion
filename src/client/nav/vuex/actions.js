export const initNav = ({ commit }, data) => {
  commit('INIT_NAV', data)
}

export const setNav = ({ commit }, data) => {
  commit('SET_NAV', data)
}

export const setSocket = ({ commit }, data) => {
  commit('SET_SOCKET', data)
}
